<style>
    /* Style pour l'image en fond */
    .container-form-page {
        height: 110vh; /* Hauteur complète de la fenêtre */
        background-image: url('/img/Trafic.webp'); /* Image de fond */
        background-size: cover; /* L'image couvre tout l'espace */
        background-position: center; /* Centre l'image */
        background-repeat: no-repeat; /* Pas de répétition */
        display: flex; /* Utilisation de flexbox */
        align-items: center; /* Centre verticalement */
        justify-content: center; /* Centre horizontalement */
    }

    /* Style pour le formulaire */
    .form-container {
        background-color: linear-gradient(88.96deg, #051039 33.47%, #003165 99.14%); /* Fond semi-transparent */
        padding: 30px; /* Espacement intérieur */
        border-radius: 8px; /* Coins arrondis */
        box-shadow: 0px 4px 10px rgba(0, 0, 0, 1); /* Ombre légère */
        width: 100%; /* Largeur relative */
        max-width: 600px; /* Largeur maximale pour le formulaire */
        opacity: 1;
    }

    /* Espacement des champs */
    .form-container .form-control,
    .form-container .btn {
        margin-bottom: 15px; /* Espacement entre les champs */
    }
</style>

<!-- Conteneur principal -->
<div class="container-form-page">
    <!-- Boîte contenant le formulaire -->
    <div class="form-container">
        <h3 class="text-center mb-2">Publier une Offre d'Emploi</h3> <!-- Titre centré -->

        {{ form_start(form, {'action': path('poster-offre-emploi'), 'method': 'POST'}) }}
        <div class="mb-3">
            {{ form_row(form.poste, {'attr': {'class': 'form-control', 'placeholder': 'Intitulé du poste'}}) }}
        </div>
        <div class="mb-3">
            {{ form_row(form.typeContrat, {'attr': {'class': 'form-control'}}) }}
        </div>
        <div class="mb-3">
            {{ form_row(form.descriptionPoste, {'attr': {'class': 'form-control', 'rows': 2, 'placeholder': 'Description du poste'}}) }}
        </div>
        <div class="mb-3">
            {{ form_row(form.modaliteTravail, {'attr': {'class': 'form-control'}}) }}
        </div>
        <div class="mb-3">
            {{ form_row(form.localisation, {'attr': {'class': 'form-control', 'placeholder': 'Lieu de travail'}}) }}
        </div>
        {{ form_end(form) }}
    </div>
</div>
