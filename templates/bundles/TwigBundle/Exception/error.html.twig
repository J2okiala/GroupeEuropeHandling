<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Erreur {{ status_code }}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="bg-light">
    <div class="container text-center py-5">
        <h1 class="display-3 text-danger">{{ status_code }}</h1>
        <p class="lead">
            {{ status_text is defined ? status_text : 'Une erreur est survenue.' }}
        </p>
        <a href="{{ path('home') }}" class="btn btn-primary mt-4">Retour à l'accueil</a>
    </div>
</body>
</html>
