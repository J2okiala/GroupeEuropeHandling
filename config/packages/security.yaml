security:
  access_denied_url: /connexion

  # Gestion des hashages des mots de passe
  password_hashers:
    Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: "auto"

  # Définition du fournisseur d'utilisateurs
  providers:
    app_user_provider:
      entity:
        class: App\Entity\Utilisateur
        property: email

  firewalls:
    dev:
      pattern: ^/(_(profiler|wdt)|css|images|js)/
      security: false
    main:
      lazy: true
      provider: app_user_provider

      # Configuration du login formulaire
      form_login:
        login_path: connexion
        check_path: connexion
        username_parameter: email
        password_parameter: password

        # Gestionnaire personnalisé pour la redirection après connexion
        success_handler: App\Security\CustomAuthenticationSuccessHandler

      # Configuration de la déconnexion
      logout:
        path: deconnexion

  # Hiérarchie des rôles
  role_hierarchy:
    ROLE_ADMIN: [ROLE_CANDIDAT]

  # Règles d'accès
  access_control:
    - { path: "^/profilCandidat", roles: [IS_AUTHENTICATED_FULLY] }
    - { path: "^/maFiche", roles: [IS_AUTHENTICATED_FULLY] }
    - { path: "^/modifierMesInformations", roles: [IS_AUTHENTICATED_FULLY] }
    - { path: "^/mesCandidatures", roles: [IS_AUTHENTICATED_FULLY] }
    - { path: "^/mesIdentifiantsDeConnexion", roles: [IS_AUTHENTICATED_FULLY] }
    - { path: "^/'postuler", roles: [IS_AUTHENTICATED_FULLY] }
    - { path: "^/supprimer-compte", roles: [IS_AUTHENTICATED_FULLY] }
    - { path: "^/confirmer_compte", roles: [IS_AUTHENTICATED_FULLY] }
    - { path: "^/profilEmployeur", roles: [IS_AUTHENTICATED_FULLY] }
    - { path: "^/filtrer-candidatures", roles: [IS_AUTHENTICATED_FULLY] }
    - { path: "^/maFicheE", roles: [IS_AUTHENTICATED_FULLY] }
    - { path: "^/mesOffresE", roles: [IS_AUTHENTICATED_FULLY] }
    - { path: "^/supprimer_offre", roles: [IS_AUTHENTICATED_FULLY] }
    - { path: "^/mesIdentifiantsDeConnexionE", roles: [IS_AUTHENTICATED_FULLY] }
    - { path: "^/supprimer_compteE", roles: [IS_AUTHENTICATED_FULLY] }
    - { path: "^/confirmer_suppression-compteE", roles: [IS_AUTHENTICATED_FULLY] }
    - { path: "^/filtrer_candidatures", roles: [IS_AUTHENTICATED_FULLY] }
    - { path: "^/telecharger_cv", roles: [IS_AUTHENTICATED_FULLY] }

when@test:
  security:
    password_hashers:
      Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
        algorithm: auto
        cost: 4
        time_cost: 3
        memory_cost: 10


