twig:
    file_name_pattern: '*.twig'
    globals:
        app_request: '@request_stack'

    # Cette option n'est pas spécifique à l'environnement, donc elle peut rester ici
    strict_variables: true
